<div class="container">
<h1> Mon Planning </h1>
<%= image_tag "eventtt.png", class:"image_bg"%>
</div>
<% (0..9).each do |av| %>
  <div class="card-planning">
  <%# <%= link_to 'Missions', new_availability_booking_path, class:"btn btn-primary"%>
  <%# <%= link_to_if availability_id != "", "", new_availability_booking_path, class: "dropdown-item" %>
    <div class="card-planning-text">
      <p class='day'></p>
    </div>
    <div>
      <label class="switch">
        <input type="checkbox" id="checkbox" data-id="<%= @availability.id %>">
        <span class="slider round"></span>
      </label>
    </div>
  </div>
<% end %>
